# 디자인 시스템 문서

> 레퍼런스: skills.sh (The Agent Skills Directory, Vercel Labs) — 비주얼 언어를 분석해 재사용 가능한 디자인 시스템으로 정리했다.
> 작성일: 2026-09-03
> 기준: 색상 코드·폰트명처럼 HTML 텍스트만으로 확정할 수 없는 값은 `(추정)`으로 표시했다.
> 페이지 구조·콘텐츠·데이터는 별도로 협업해 정한다. 이 문서는 컨셉·컬러·타이포그래피·레이아웃·컴포넌트·UX 패턴만 다룬다.

---

## 1. 디자인 컨셉

**"터미널 위에 얹은 디렉토리"**

- 개발자 대상 서비스답게, 전체를 **CLI/터미널 감성**으로 통일한다.
- 핵심 액션은 버튼보다 한 줄짜리 명령어/코드 형태로 노출한다.
- 장식 요소를 거의 쓰지 않는 **미니멀·모노크롬** 스타일. Vercel 계열 사이트(vercel.com, nextjs.org)의 디자인 언어(Geist)를 따른다.
- 정보 밀도가 높은 **리스트/테이블 중심 레이아웃**을 지향한다.

---

## 2. 비주얼 토큰

### 2.1 컬러 `(추정 — Vercel Geist 팔레트 기준)`

| 역할 | Light | Dark |
|---|---|---|
| 배경 | `#FFFFFF` | `#000000` |
| 서피스(카드/코드박스) | `#FAFAFA` | `#0A0A0A` |
| 보더 | `#EAEAEA` | `#333333` |
| 본문 텍스트 | `#171717` | `#EDEDED` |
| 보조 텍스트 | `#666666` | `#A1A1A1` |
| 강조(링크/CTA) | `#0070F3` 또는 텍스트와 동일 | 동일 |

- 컬러 액센트를 거의 쓰지 않는다. 위계는 **명도 대비**로만 만든다.
- 다크 모드를 기본 지원한다.

### 2.2 타이포그래피 `(추정)`

| 용도 | 폰트 |
|---|---|
| 본문/UI | Geist Sans |
| 코드·명령어·숫자 | Geist Mono |

- 설치 수(`3.2M`)처럼 숫자가 열을 이룰 때는 `font-variant-numeric: tabular-nums` 적용 `(추정)`.
- 헤딩 위계: H1(서비스명) → H2(섹션 제목) → H3(하위 그룹 제목).

### 2.3 간격·레이아웃

- 최대 콘텐츠 폭 약 `960~1080px`, 중앙 정렬 단일 컬럼 `(추정)`.
- 섹션 간 세로 여백 크게(64~96px), 섹션 내부는 촘촘하게.
- 밀도 높은 리스트/테이블 행은 낮은 높이로 — 스캔 가능함을 우선한다.
- 반응형: 헤더 내비 → 토글 버튼, 다열 그리드 → 2열/1열 스택.

### 2.4 컴포넌트 요약

| 컴포넌트 | 특징 |
|---|---|
| 코드 박스 | 모노 폰트, 얇은 보더, `$` 프롬프트, 복사 버튼 |
| 키보드 배지 | 작은 라운드 사각형, 보더만, 단축키 힌트 |
| 탭 | 텍스트 탭 + 활성 탭 밑줄/굵기 변화, 괄호 안 카운트 |
| 랭킹/리스트 테이블 | 행 전체 링크, hover 하이라이트, 접힘 행("+N more…") |
| 로고·태그 마퀴 | 2벌 복제 + `translateX` 무한 애니메이션 |
| 링크 그리드(푸터 등) | 다열, 소제목 + 리스트 |

---

## 3. UX 패턴

1. **한 줄 명령이 곧 CTA** — 회원가입·다운로드 버튼 대신 명령어/코드를 복사하게 만든다.
2. **키보드 우선** — 단축키로 핵심 인터랙션(검색 등)에 빠르게 접근한다.
3. **리스트 압축** — 같은 그룹의 항목을 접어 다양성을 유지하면서 순위 정보는 보존한다.
4. **집계 숫자 노출** — 전체 합계와 그룹 합계를 함께 보여줘 규모감을 전달한다.
5. **호환성/커버리지 시각화** — 마퀴 등으로 "어디서나 쓸 수 있다"를 텍스트 없이 전달한다.
